{
  "name": "agent-memory-hub",
  "version": "1.1.0",
  "description": "Persistent, intelligent, searchable long-term memory for AI agents",
  "type": "module",
  "engines": {
    "node": ">=18"
  },
  "bin": {
    "agent-memory-hub": "./build/index.js"
  },
  "scripts": {
    "build": "tsc",
    "start": "node build/index.js",
    "dev": "tsc --watch",
    "test": "tsc && node build/test.js"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.3.0"
  }
}
